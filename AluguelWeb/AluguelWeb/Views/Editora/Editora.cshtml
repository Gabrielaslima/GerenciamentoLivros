@model IEnumerable<AluguelWeb.Models.Entidades.Editora>

@{
    ViewData["Title"] = "Editora";
}

<h2>Editora</h2>

<p>
    <a asp-action="CadastroEditora">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                <a asp-action="EdicaoEditora" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="DetalhesEditora" asp-route-id="@item.Id">Details</a> |
                <a asp-action="DeletarEditora" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
